// ===== Config =====
  const CFG = {
    tickMs: 1000,
    gameMinutePerTick: 1,
    cropUpgradeBase: 10, // Chi phí lúa nâng cấp (áp dụng chung cho mọi công trình, trừ Ruộng lúa)
    trainSecPerUnit: 3,
    warehouseBase: 800,
    warehousePerLv: 250,
    granaryBase: 800,
    granaryPerLv: 250,
    mapSize: 21,
  };